<%% get<%=fName%>Data = (key) => {
  const <%=fName%>Data = {
    default: {
      modifier: '',
    }
  };

  if (typeof key === 'undefined') {
    return <%=fName%>Data;
  }

  const result = <%=fName%>Data[key];

  if (typeof result === 'undefined') {
    throw new Error(`<%=name%>-data: ${key} is invalid key name!`);
  }

  return result;
}
<%- end %>%>
