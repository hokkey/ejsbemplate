<%
// grid-list/_grid-list-guide.ejs
// A preview HTML of "grid-list" component
  const EjsRoot = '../../';
  const Block = 'grid-list';
  include(`./_${Block}-data`);
%>
<% Object.entries(getGridListData()).forEach((param) => { %>
  <%- include(...util.inc(EjsRoot,Block,{type: param[0]})); %>
<% }); %>
