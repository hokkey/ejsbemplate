<%
// site-header/_site-header.ejs
// A main HTML file of "site-header" component
//
  const Block = 'site-header';
  const EjsRoot = '../../';
  if (typeof siteHeaderParams === 'undefined') { siteHeaderParams = {}; }
  const params = siteHeaderParams;

  let dataParams = {};
  // Un-comment below to include a component specific data
  //if (typeof params.type === 'undefined') { params.type = 'default'; }
  //include(`./_${Block}-data`);
  //dataParams = getSiteHeaderData(params.type);

  let defaults = {
    modifier: '',
    text: 'Default text here',
    //href: '',
  };

  const p = util.init()(util, params, defaults, dataParams);
%>
<!-- <%=Block%> -->
<div class="<%=Block%><%-util.setModifier(Block, p.modifier);%>"<%#util.printAttr('href', p.href);%>>
</div>
<!-- /<%=Block%> -->
