<%
// site-header/_site-header-guide.ejs
// A styleguide document of "site-header" component
  const EjsRoot = '../../';
  const Block = 'site-header';
  const data = {
    pageSettings: sitePages['dummy'],
    root: '../../public/'
  };
  if (typeof util === 'undefined') { include(`${EjsRoot}util/_util`); util = getUtil(); }
%>
/*
#overview
site-header
*/
/*
#styleguide
.site-header
```
<%-include(`${EjsRoot}components/site-header/_site-header-preview`);
```
*/
