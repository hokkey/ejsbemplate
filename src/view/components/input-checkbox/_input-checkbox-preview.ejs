<%
// input-checkbox/_input-checkbox-guide.ejs
// A preview HTML of "input-checkbox" component
  const EjsRoot = '../../';
  const Block = 'input-checkbox';
%>
<%- include(...util.inc(EjsRoot,Block,{modifier:''})); %>

<%# Object.entries(getInputCheckboxData()).forEach((param) => { %>
  <%#- include(...util.inc(EjsRoot,Block,{type: param[0]})); %>
<%# }); %>
