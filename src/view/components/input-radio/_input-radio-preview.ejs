<%
// input-radio/_input-radio-guide.ejs
// A preview HTML of "input-radio" component
  const EjsRoot = '../../';
  const Block = 'input-radio';
%>
<%- include(...util.inc(EjsRoot,Block,{modifier:''})); %>

<%# Object.entries(getInputRadioData()).forEach((param) => { %>
  <%#- include(...util.inc(EjsRoot,Block,{type: param[0]})); %>
<%# }); %>
