<%
// input-radio/_input-radio.ejs
// A main HTML file of "input-radio" component
//
  const Block = 'input-radio';
  const EjsRoot = '../../';
  if (typeof inputRadioParams === 'undefined') { inputRadioParams = {}; }
  const params = inputRadioParams;

  let dataParams = {};
  // Un-comment below to include a component specific data
  //if (typeof params.type === 'undefined') { params.type = 'default'; }
  //include(`./_${Block}-data`);
  //dataParams = getInputRadioData(params.type);

  let defaults = {
    modifier: '',
    label: 'Option',
    id: `dummy-radio-${util.randomStr()}`,
    inputName: 'dummy-radio',
    attrOption: ''
  };

  const p = util.init()(util, params, defaults, dataParams);
%>
<!-- <%=Block%> -->
<span class="<%=Block%><%-util.setModifier(Block, p.modifier);%>">
  <input class="<%=Block%>__input" id="<%=p.id%>" type="radio" name="<%=p.inputName%>" <%-p.attrOption%>/>
  <label class="<%=Block%>__label" for="<%=p.id%>">
    <span class="<%=Block%>__radio"></span>
    <span class="<%=Block%>__text"><%-p.label%></span>
  </label>
</span>
<!-- /<%=Block%> -->
