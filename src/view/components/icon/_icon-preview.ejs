<%
// icon/_icon-guide.ejs
// A preview HTML of "icon" component
  const EjsRoot = '../../';
  const Block = 'icon';
%>
<%- include(...util.inc(EjsRoot,Block,{modifier:''})); %>

<%# Object.entries(getIconData()).forEach((param) => { %>
  <%#- include(...util.inc(EjsRoot,Block,{type: param[0]})); %>
<%# }); %>
