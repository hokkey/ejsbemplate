<%
// text-list/_text-list-guide.ejs
// A styleguide document of "text-list" component
  const EjsRoot = '../../';
  const Block = 'text-list';
  const data = {
    pageSettings: sitePages['dummy'],
    root: '../../public/'
  };
  if (typeof util === 'undefined') { include(`${EjsRoot}util/_util`); util = getUtil(); }
%>
/*
#overview
text-list
*/
/*
#styleguide
.text-list
```
<%-include(`${EjsRoot}components/text-list/_text-list-preview`);
```
*/
