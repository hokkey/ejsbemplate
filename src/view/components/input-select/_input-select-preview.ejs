<%
// input-select/_input-select-guide.ejs
// A preview HTML of "input-select" component
  const EjsRoot = '../../';
  const Block = 'input-select';
%>
<%- include(...util.inc(EjsRoot,Block,{modifier:''})); %>

<%# Object.entries(getInputSelectData()).forEach((param) => { %>
  <%#- include(...util.inc(EjsRoot,Block,{type: param[0]})); %>
<%# }); %>
