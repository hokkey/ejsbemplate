<%
// site-footer/_site-footer.ejs
// A main HTML file of "site-footer" component
//
  const Block = 'site-footer';
  const EjsRoot = '../../';
  if (typeof siteFooterParams === 'undefined') { siteFooterParams = {}; }
  const params = siteFooterParams;

  let dataParams = {};
  // Un-comment below to include a component specific data
  //if (typeof params.type === 'undefined') { params.type = 'default'; }
  //include(`./_${Block}-data`);
  //dataParams = getSiteFooterData(params.type);

  let defaults = {
    modifier: '',
    text: 'Default text here',
    //href: '',
  };

  const p = util.init()(util, params, defaults, dataParams);
%>
<!-- <%=Block%> -->
<div class="<%=Block%><%-util.setModifier(Block, p.modifier);%>"<%#util.printAttr('href', p.href);%>>
</div>
<!-- /<%=Block%> -->
