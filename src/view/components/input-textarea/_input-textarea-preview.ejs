<%
// input-textarea/_input-textarea-guide.ejs
// A preview HTML of "input-textarea" component
  const EjsRoot = '../../';
  const Block = 'input-textarea';
%>
<%- include(...util.inc(EjsRoot,Block,{modifier:''})); %>

<%# Object.entries(getInputTextareaData()).forEach((param) => { %>
  <%#- include(...util.inc(EjsRoot,Block,{type: param[0]})); %>
<%# }); %>
