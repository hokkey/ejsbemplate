<%
// input-textarea/_input-textarea.ejs
// A main HTML file of "input-textarea" component
//
  const Block = 'input-textarea';
  const EjsRoot = '../../';
  if (typeof inputTextareaParams === 'undefined') { inputTextareaParams = {}; }
  const params = inputTextareaParams;

  let dataParams = {};
  // Un-comment below to include a component specific data
  //if (typeof params.type === 'undefined') { params.type = 'default'; }
  //include(`./_${Block}-data`);
  //dataParams = getInputTextareaData(params.type);

  let defaults = {
    modifier: '',
    id: `dummy-textarea-${util.randomStr()}`,
    inputName: 'dummy-textarea',
    placeholder: '',
    attrOption: '',
    val: ''
  };

  const p = util.init()(util, params, defaults, dataParams);
%>
<!-- <%=Block%> -->
<textarea class="<%=Block%><%-util.setModifier(Block, p.modifier);%>" id="<%=p.id%>" name="<%=p.inputName%>" placeholder="<%=p.placeholder%>"<%-util.printWithSpace(p.attrOption)%>><%=p.val%></textarea>
<!-- /<%=Block%> -->
